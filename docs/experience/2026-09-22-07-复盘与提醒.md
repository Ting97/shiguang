# 体验轮训报告：复盘（日/周/月/年）+ 提醒（2026-09-22 07 轮，手动提前触发）

- 轮次：定时轮训第 5 轮（用户要求"本轮完成后自动进入下轮"，本轮手动即时执行）
- 环境：本地 standalone :3100（AUTH_DISABLED=1），移动 375，暗色主题
- 产物：修复 1 项（F8 AI 日小结「生成于」UTC 时区显示）

## 一、体验结论

**复盘（/schedule 日历 tab 内四视图）：**
- 四视图（日/周/月/年）各自挂对应复盘卡：日小结（一句总结+亮点+💡建议三段结构）、周报（7 天条带 + AI 周报）、月报/年报同构。
- 空态正确：无记录时「生成小结/生成本周小结」禁用并解释；造数后按钮启用。
- AI 日小结真调成功（~50s）：「今日工作专注，但未完成待办，心情平静」+ 亮点 + 建议，质量可读。
- 时间轴未记录区随数据自动分段（00:00–09:00 / 11:00–24:00）✓。
- **bug（已修 F8）**：卡片头「生成于 09-21 22:53」显示 UTC 时间（ISO 直接 slice），应为北京 06:53。
- 测试伪象记录：旧标签 goto 后文档未刷新导致"API 有数据 UI 空"的假象（页面内 fetch 与新标签均正常），非产品 bug——IAB 陈旧标签问题再+1。

**提醒（首页横幅）：**
- 设计确认：提醒待办在 remind_at（到期前 15 分钟）到达后才出现，此前不显示是正确行为。
- 横幅渲染完整：🔔 提醒区块 + 「待办即将到期：…（HH:MM）」+ 「知道了 ✕」关闭 + ☀️ 今日快捷标记 + 锚点跳转 #todos；标记今日后与今日行动计数联动 ✓。

## 二、本轮迭代

- **F8**：apps/web/src/app/calendar/day-review-card.tsx「生成于」时间戳 +8h 转北京时间（与 spaces/[id]/detail.tsx 的 bjDate 惯例一致）。周/月/年卡未展示 generated_at，无需同改。

## 三、建议

- P3：周报条带的每天「—」在当日有点击定位到该日视图的交互（button 语义已具备，确认跳转是否实现）。
- P3：AI 小结生成期间按钮无 busy 文案变化（生成 ~50s，建议显示「生成中…」防重复点击——本轮用代码层确认 busy 态存在但视觉反馈弱）。
