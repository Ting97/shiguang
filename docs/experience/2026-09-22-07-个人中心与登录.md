# 体验轮训报告：个人中心 + 登录注册（2026-09-22 07 轮·定时档）

- 轮次：定时轮训第 6 轮（07:05 档）
- 环境：本地 standalone :3100（AUTH_DISABLED=1）移动 375 + 生产只读复核
- 产物：修复 1 项（F9 用量列表无名模型兜底「其他模型」）

## 一、个人中心（/profile）

- 头部卡：头像字、昵称、掩码手机（138****1111）+「已验证」「管理员」徽章、加入日期，信息完整。
- 昵称编辑：变更后「保存」启用、恢复原值自动禁用 ✓（脏检查正确）。
- 修改密码：三字段；「更新密码」按钮只要字段非空即启用，但**提交时**有「两次输入的新密码不一致」内联校验（page.tsx:86），服务端亦有 ≥8 位校验（auth/profile/route.ts:41）——按钮不预拦属可接受设计，误报排除。
- 套餐与 AI 用量：免费版·管理员不限量、近 30 天汇总、按模型分项（glm-4-flash / glm-5.3-flash / glm-asr-2512）+ 内测说明文案。
- **bug（已修 F9）**：audit_logs 中 model 为空的 5 条记录让用量列表出现一行无名模型（渲染为空白）。已加兜底「其他模型」。
- 导出：JSON（application/json; charset=utf-8, 3.1KB）与 Markdown（text/markdown; charset=utf-8 + 日期文件名）响应头规范；Git Bash curl 显示乱码纯属终端编码，非产品问题。
- 后台管理入口卡（管理员可见）正常指向 /admin。

## 二、登录注册（/login，代码级 + 生产只读）

- 本地 AUTH_DISABLED=1 下 /login 自动跳首页，无法黑盒走真实验证码流；生产环境带历史登录态同样跳转。已做代码级审读：手机号 `^1[3-9]\d{9}$` 与邮箱双正则校验、60 秒倒计时、内联错误文案、登录页视觉（SVG 光束插画）。未发现明显问题。
- 真实注册→验证码→登录的全流程建议在关闭 AUTH_DISABLED 的环境人工走一遍（本轮未覆盖，非阻塞）。

## 三、生产只读观察（附带）

- 生产账号存在过期待办「看牙（15:00）」：提醒横幅正确标「待办已过期」；因截止日在昨天，今日行动不显示它（口径一致），但用户在清单里缺少"过期未完成"的显式入口——建议 P3：今日行动或 TODO 今日视图并入过期未完成条目。

## 四、本轮迭代

- **F9**：apps/web/src/app/profile/page.tsx 用量模型名兜底 `{m.model || "其他模型"}`。3100 重载后验证显示「其他模型 · 近30天 5 次」✓。根因（审计写入时 model 空串）属 API 侧数据规范，可在后续轮次追查写入点。

## 截图索引

- login-prod：生产首页（只读，含提醒横幅过期示例）
