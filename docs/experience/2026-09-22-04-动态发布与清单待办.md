# 体验轮训报告：动态发布 + 清单待办（2026-09-22 04 轮）

- 轮次：定时轮训第 2 轮（04:10 档，实际 04:11 起测）
- 环境：本地 standalone :3100（AUTH_DISABLED=1，暗色主题为主），移动 375×812 优先
- 产物：修复 2 项（F3 chips 滑动渐隐提示、F4 ⋯ 菜单顶部避让）；误报清理 2 项

## 一、视觉 UI 感受

**做得好：**
- 发布弹层「记录此刻」：placeholder 带示例文案、发布键禁用态清晰、🖼 相册入口标注「最多 9 张」、「发布后 AI 自动识别」预期管理到位。
- todo-board 行操作菜单卡片：任务标题做弹层头，六项操作带图标+「已开启」徽标+灰色 hint（如「今日标记跨零点自动失效」「其行动将一并删除」），自解释性好。
- 行动详情面板：标题/1000 字描述（实时计数）/截止时间/🔁 每日（说明文案完整），保存取消明确。
- AI 拆解结果：进度徽标 0/5 → 完成一条 1/5 实时更新，行动动宾结构质量高（明确验证目标/准备验证工具/执行验证步骤/记录验证结果/整理验证报告）。
- 暗色主题下整页协调，无对比度问题。

**问题：**
- 智能列表第 4 个 chip「✓ 已完成」在 375 宽完全不可见且无滑动提示（F3，已修）。
- 历史脏数据标题渲染为乱码方块（数据问题非产品 bug，已清理数据）。

## 二、功能体验（实测）

| 路径 | 结果 |
| --- | --- |
| 发布：纯空白文本 | ✅ 发布键保持禁用 |
| 发布：纯 emoji（😀🌙✨） | ✅ 可发布 |
| 发布：600 字长文 | ✅ maxLength=2000 内完整接受，卡片完整渲染无截断；无字数计数器（P3 建议：接近 2000 时显示 n/2000） |
| 发布长文 → feed | ✅ toast + 今天分组 + AI 异步识别 |
| 添加任务（今日视图下） | ✅ 自动标记今日（产品设计：视图语境带入 body.today），今日计数 1→2 |
| ⋯ 菜单卡 | ✅ 六项操作完整，徽标/hint 正确 |
| ✨ AI 拆解 | ✅ 真调成功生成 5 条行动（首次因本地服务缺 ZHIPUAI_API_KEY 失败——测试环境问题，重启带环境变量后通过；audit_logs 有完整记录） |
| 行动 ✓ 完成 | ✅ 进度 0/5→1/5 |
| 行动详情面板 | ✅ 全字段正常 |

未覆盖（与既往轮次结论一致）：语音长按（自动化限制）、图片上传（IAB 无文件选择器，真实设备已于 001 批次验证）、拍照入口（pointer:coarse 设备专属）。

## 三、BUG 清单

- **B1（P2，本轮已修 F3）**：智能列表 chips 行（todo-board 移动端横滑容器）在 375 宽下 scrollWidth 436 > clientWidth 335，「✓ 已完成」完全在视口外；滚动条隐藏且无渐隐/箭头提示，功能不可发现。复现：/schedule?tab=todo 观察第一行 chips（截图 shots/2026-09-22-04-todo-board-dark.png）。
- **B2（P3，本轮已修 F4）**：动态卡片「⋯」菜单定位只有底部 clamp，无吸顶导航避让；卡片紧贴导航时菜单可能被盖（预防性修复，一行 clamp）。
- **B3（数据卫生，已清理）**：本地库一条早期测试数据 title 编码损坏（「???专业?一?复习」），渲染为乱码方块；确认 DB 值即坏（psql 直查），非前端问题，已删除。
- **误报销案（遗留池）**：①「feed 末条被 FAB 遮挡」——实测首页容器已有 pb-28（112px）> FAB 高度需求（76px），不成立；②「搜索 placeholder 375 截断」——实测完整显示，不成立。

## 四、本轮迭代记录

- **F3**：apps/web/src/components/todo-board.tsx chips 行加右缘 28px 渐隐 mask（`[mask-image:linear-gradient(...)]`），提示可横滑；375 下 computed maskImage 已生效、渐隐可见（shots/2026-09-22-04-chips-fade-fix.png）。
- **F4**：apps/web/src/components/moment-feed.tsx ⋯ 菜单 top 加 `Math.max(60, …)` 避开吸顶导航。
- 回归：/schedule 全页加载、chips 切换、任务行渲染、1/5 进度显示均正常；tsc 通过。
- 附带发现与修复：3100 验证环境曾出现「cp -r out 嵌套（out/out）」导致服务供旧构建——已 rm 后重拷；部署红线补充为「**cp 前先 rm -rf 旧 out**」（已写入 STATE.md）。

## 截图索引

- publish-longtext：600 字长文发布结果（暗色）
- todo-board-dark / todo-after-add：清单页现状（B1 现场、自动今日）
- todo-row-menu：行操作菜单卡片
- todo-decompose2 / todo-actions-expanded：AI 拆解结果与展开
- chips-fade-fix：F3 修复后渐隐效果
