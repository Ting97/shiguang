# 体验轮训报告：/admin 后台（2026-09-22 07 轮·定时档）

- 轮次：定时轮训第 7 轮（07:25 档）
- 环境：本地 standalone :3100（AUTH_DISABLED=1，开发者=管理员）移动 375，暗色主题
- 产物：本轮无代码改动（全链路均正常），纯体验报告

## 一、AI 管理 tab

- **Prompt 清单**：16 个 key 按「全量五域 / 单域×6 / 复盘四档 / 画像合并 / 空间分类 / 待办拆解 / 行动细化 / 优化器」分组排列，选中即载入编辑器；顶部💡横幅一句话讲清生效/回滚/优化三件事，设计成熟。
- **编辑→保存闭环**：追加一行保存 → toast「已保存并即时生效」→ 清单项徽标「代码默认」→「DB 覆盖生效中」→ 详情区「🟢 DB 覆盖 · 更新于 09/22 07:26」（北京时间正确）✓。
- **版本历史**：保存后「版本历史 (1)」→ 面板「最近 30 条 · 07:26 · 290 字 · 开发者 · 载入」✓。
- **恢复代码默认**：一键恢复（confirm 确认）→ 状态回「代码默认」，版本历史保留 ✓。改坏可回滚的承诺成立。
- **✨AI 优化真调**（space_classify，意图「输出更简洁，减少限制词」）：约 55 秒返回建议稿，出现「采纳 / 放弃」双按钮 + 提示「采纳后仅填入编辑器，检查无误再手动保存」；实测采纳只改编辑器内容（257→238 字），状态仍「代码默认」——与规格完全一致 ✓。

## 二、营销管理 tab

- 用户套餐管理：用户列表（免费·30天42次）+ 升级 Pro(1年) 按钮（未实测升级，避免改本地套餐状态）。
- 邀请管理：说明文案（一码一人、凭码注册入口）清晰；「生成邀请码」实测成功（ZYF23793，30 天有效期，admin API 可查）；无删除接口（设计为自然过期），测试码已从本地库清理。
- Token 消耗：全部累计/近 30 天双窗口 + 按模型统计表；admin 视图把空模型行正确标注为「(离线规则·无模型)」——与 profile 页（上轮 F9 前空白）形成对照，F9 修复方向正确。
- 被邀请人空态 ✓。

## 三、BUG 与建议

- 无新增 BUG。两处 P3 建议：
  1. AI 优化生成期间（~55s）按钮建议显示「优化中…」并禁用，防连点（本轮 busy 态存在但按钮文案未变化，视觉反馈弱）。
  2. 邀请码支持作废（可选）：当前只能等 30 天过期，若误生成/泄露无法主动失效。

## 四、迭代

- 无（本轮未发现需要改代码的问题；F9 修复于上一轮已部署）。
