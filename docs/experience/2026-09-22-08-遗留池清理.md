# 遗留问题池清理批次（2026-09-22 08 轮，定时任务暂停后手动执行）

背景：用户指示暂停定时轮训、集中处理遗留问题池。本轮完成 4 项，销案 4 项。

## 已完成

### ① 过期待办并入今日清单（P2，来源：06 轮生产观察）
- `apps/api/src/app/api/todos/route.ts`：
  - today 视图：`today_tag_date = 今天` 放宽为 `或 due_at 北京日期 < 今天`（仅顶层待办）。
  - today-actions：行动分支加 ⑤ 行动自身已过期；顶层待办分支拆为「今天到期（含 done）/ 过期未完成」两条件。
  - 计数徽标 counts.today 同步含过期，徽标与列表一致。
- 前端零改动：dueTag 早有「已过期」红标（todo-bits.tsx:19）。
- 验证：API 断言今日视图/今日行动均含过期待办且 counts.today=1 ✓；浏览器今日行动显示红标「已过期」✓（shots/2026-09-22-08-feed-clamp.png）。

### ② 账户负余额红示（P3）
- `apps/web/src/app/finance/page.tsx`：余额 < 0 渲染红色 `-¥N ⚠`，title 引导「点管理调整期初余额」。
- 验证：支付宝期初 -50 → 卡片显示红字「-¥50 ⚠」✓（shots/2026-09-22-08-finance-negative.png）。

### ③ feed 长文折叠展开（P3）
- `apps/web/src/components/moment-feed.tsx`：raw_text > 150 字默认 `line-clamp-6` 收起，独立「展开全文（N 字）/收起」按钮（stopPropagation，不与"点原文弹识别菜单"抢交互）。
- 验证：725 字动态折叠显示省略号 + 展开按钮；点击展开全文显示、收起可回 ✓（shots/2026-09-22-08-feed-clamp.png / feed-expanded.png）。

### ④ 销案两项（复测已存在）
- AI 优化按钮 busy 文案：admin-ai-panel.tsx:239 已有「优化中…」（07 轮看漏）。
- AI 日小结 busy 文案：day-review-card.tsx:50 已有「解读中…」（05 轮看漏）。

## 验证与交付
- tsc 双端通过；API 断言 2/2；浏览器验证 3/3。
- 测试数据已清理（过期待办、长动态、支付宝期初复原）。
- commit 后双 tar 部署（API+Web 均有改动），冒烟 /login 200。
