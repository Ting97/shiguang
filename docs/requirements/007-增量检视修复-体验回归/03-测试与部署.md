# 007 · 增量全仓检视修复 + 财务模块体验回归（2026-09-24）

> 批次性质：在 006（全仓检视 + 财务深度体验）之上的一轮**独立增量检视**。六组并行深检（api server 域层 59 文件、86 路由、web 页面 63 文件、web 组件/shared/lib 121 文件、packages 三包 75 文件、006 提交全量 diff 复核）共产出约 50 项新发现，与 006 修复清单去重后保留 40 项，本批集中修复。无迁移、无 DB 变更（schema.sql 基线注释性同步除外，不参与运行）。

## 一、P0/P1 修复

| 级别 | 问题 | 修复 |
| --- | --- | --- |
| P0 | `moment-feed/kit.ts` 的 `combineHM` 用本地 `setHours`（与 `lib/bj-time.ts` 北京版同名冲突），动态卡内编辑日程块保存即偏移 `(8−本地偏移)` 小时，每次编辑漂移一次（海外设备静默数据损坏；calendar-panel/today-schedule 同型 bug 已修，唯独这份漏改） | 删除 kit 本地版，`recognition-blocks.tsx` 改 import `@/lib/bj-time`（单源收敛） |
| P1 | `review-quota.timeCeiling` 把 trading 档的 `periodKey`（=账号 **UUID**）与「周一日期串」做字典序比较：约 81% 的账号恒大于任何日期串 → `ceiling=0` → 生成 AI 交易复盘**永久 429**；能否成功取决于 UUID 首字符 | `case "trading"` 拆出直接 `return M`（不做时间解锁，次数上限仍由 used 统计生效） |
| P1 | 动态段 id 未过 `assertUuidParam` 直落 uuid 列 → 22P02 → 500（~19 个路由：feed/entries×6/blocks/spaces×3/todos×2/contacts×3/interactions/accounts） | 逐 handler 补 `assertUuidParam`（entries/images 连带 `imageId`、reflections 连带 `rid`），与 006 已迁移的 transactions/debts/trading 对齐 |
| P1 | contacts 全模块（页面+详情+两表单）import 裸 `@/lib/client-api`，绕过 401 统一收口 → 会话过期后页面假死、永不跳登录 | 4 处改 `@/shared/api`（ApiClientError 一并切换） |
| P1 | `day-timeline.tsx` 时间轴用本地零点/本地时钟定位北京日期的块：海外设备块位置、红线、当日结构环全错，且与列表视图（北京口径）同屏矛盾；`calendar-panel.dayStartMs` 同病 | dayStart 改 `T00:00:00+08:00` 锚，minOfDay/nowMin 全部北京口径，红线初值 `null` 挂载后校准（顺带修水合首帧） |
| P1 | `prompt.ts` 金标准示例输出 `"今天07:30"`/`"明天15:00"` 非法日期串，模型模仿 → 整包校验必失败烧一次修复重问（招牌例句「明天下午三点看牙」必中） | 示例改 DOMAIN_PROMPTS 已有的 `<今天日期>T07:30` 占位约定 |
| P1 | 混合引擎 `title` 契约矛盾：瘦身契约 max(40) 直传全量契约 max(30) → zod 抛错且 hybrid 路径无降级 try → 识别请求 500 | mapAiResult/rulesPipeline title `slice(0,30)`；hybrid 空标题回落 `makeTitle` |
| P1 | `packages/db/runner.ts` 空库忘加 `--fresh` 时静默回填 38 条迁移记录 → 零业务表 + 「已是最新」，`--fresh` 判 `done.size===0` 永久跳过，新环境单向损坏 | 回填前 `to_regclass('profiles')` 探测，真空库报错提示 `--fresh` |
| P1 | `voice-recorder.begin()` 在 `await getUserMedia` 期间可重入：泄漏第一路麦克风流（指示灯常亮）+ 秒表 interval 翻倍且停不掉 | 进入即同步置 `requesting` 占位，早退/catch 回滚，catch 补 `tracks.stop()`；capture-button 蒙层排除该态 |

## 二、P2 修复（摘要）

- **API**：`review-ctx` 年报默认年份本地 getter（buildChain 同型一并修）；`export` md 导出 localYmd/localClock 本地时区（UTC 宿主错 8h）+ entries 预转换二次偏移；people AI 502 泄漏上游错误串 → 固定文案+日志；goal createTodo/updateTodo 的 parentId/spaceId/afterId/activityId、timeline patchFeed spaceId 非 uuid 500 → 400；appendManual mood score NaN/越界 500 → 400；confirmPending 盲信快照 Invalid Date → 400 引导重识别；createBlock activityId FK 23503 → 400「类别不存在」（对齐 updateBlock）；autoCheckAfterPayment 单笔额 vs 注释「当月合计」不符 → 改查当月合计；login-guard `interval '${…}'` 字面量化（全仓唯一非参数化点）；transactions/import 分 chunk 无事务（中途失败半量入库）→ 整包 begin/commit；accounts/finance-overview 余额 `::int` 溢出（>2147 万单账户整页 500）→ `::bigint`+Number 映射；billing/plan userId、prompts/restore versionId、transactions/import accountId 补校验；admin prompts preview `limit ${…}` 参数化（006 后仅存的一处拼接）；reflections `?limit=1.5` LIMIT bigint 500 → trunc+offset 上限
- **packages**：glm.ts sdk 传输无预算重试（与 legacy 5 次退避不等价）→ 共用 `chatWithRetry`；空 content 可重试；extractJson 改扫描全部围栏逐个配平；混合引擎心情置信度 `Math.max(p,0.6)` 抬轿 → 缺失才 0.6；规则兜底 `/随/` 误命中「随便」→ 收紧为 `随(礼|份子)|礼金|份子|红包`；「大后天」+2 → +3；「晚上12点」→ 次日 0 点；DietItemV2.name min(2) 拒单字食物名 → min(1)；schema.sql 基线 check 同步 019/022（people/ai）；social.ts 2/29 生日平年滚到 3/1 → 取 2/28
- **Web**：备付取消勾选只发组内首笔 id 静默无效（006 ❌ 遗留）→ 取消时按行全量 `liabilityIds` 循环 PUT；trades-section 筛选/翻页竞态 → seq 守卫；finance 确认流水/全部入账双击双发 → busy 守卫 + chips disabled；spaces 新建默认开始日期本地日 → `bjToday()`；profile 页 me/plan 失败永久假死 → 错误态+重试，保存昵称 `setTimeout(reload)` → `await refresh()`；publish-sheet 发布失败强制关面板丢长文 → 保留面板就地报错；trading 日列表 375px 溢出 → `overflow-x-auto`

## 三、财务模块深度体验回归（GUI 实测）

本地 3100 standalone 真实链路（测试库 + AUTH_DISABLED=1），种子数据：4 账户 / 16 笔当月+上月流水 / 预算 8000 / 3 负债+1 还款 / MT5 交易账号 20 笔（scripts/qa/seed-finance-gui.mjs，可重复执行）。

- **PC 1440×900**：记一笔全链路（空金额禁用 → 提交「保存中…」防重 → 关表单 → 列表/账户/分类/预算四区联动刷新）；总览账户余额、储蓄率、超支警示（344%）正确；负债页备付勾选/取消勾选均持久化（本次组语义修复验证）；交易页账号 chips/汇总/每日柱状/权益曲线正常
- **移动端 390×844**：四个子页（概览/负债/收支复盘/交易）无横向溢出（scrollWidth≤382）；底部抽屉式记账弹层布局完整；顶栏五项 DOM 几何无裁切；每日盈亏表右侧裁切问题已由本批 `overflow-x-auto` 修复
- **回归**：日程页时间轴红线落在北京时刻 05:53（本批时区修复实测通过）

## 四、验证与部署

- typecheck（web/api/ai 三端）0 error；eslint 0 error
- `npm test`：api 76（+test 库）/ ai **101**（新增 5 回归：大后天、晚上12点、40字标题截断、随便误判、2/29 生日）/ shared 42，**0 fail**
- `test:routes` **15/15**（注：dev 冷启动首次请求可能编译超时假挂，重跑即绿；两例 409/400 假失败为被杀残留数据，恰证冲突/查重逻辑生效）
- `npm run poc`：39/46（85%）与基线一致
- `npm run build` 通过；无迁移、无 DB 变更，双 tar 部署 + 冒烟

## 五、遗留（记录不阻塞）

- 时间块重叠校验 check-then-insert 在双端并发下仍可击穿（需 advisory lock/exclusion constraint，涉及面大）
- 复盘生成 acquireGeneration 无并发锁（双击可烧双份配额，需行锁）
- 登录接口存在账号存在性时序侧信道（需假哈希对齐耗时，属安全加固）
- 纯静态导出水合 #418（005 已记录）：calendar-panel/day-timeline 首帧烘焙构建时刻，本批已在 day-timeline 侧缓解（nowMin 挂载后校准）
- 首页 resetSearch 双请求、moment-link 逐键无防抖、use-home-data loadMore stale 误报失败（均有 seq 兜底，仅浪费）
