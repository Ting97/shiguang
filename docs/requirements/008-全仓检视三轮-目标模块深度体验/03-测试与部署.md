# 008 · 全仓检视三轮 + 目标主线深度体验（PC/移动端）（2026-09-24）

> 批次性质：在 006/007 两轮全仓检视之上的第三轮。六组并行深检（api 域层 61 文件、89 路由、web 页面+lib、web 组件 99 文件、packages 三包、交叉一致性/工程卫生）产出约 45 项新确认发现，本批集中修复约 30 项；另对**目标主线（空间/待办板）**做 PC 1440×900 + 移动端 390×844 GUI 深度体验，发现并修复 2 项视觉/交互缺陷、验证多项核心链路。**无迁移、无 DB 变更**。

## 一、API 侧修复

| 级别 | 问题 | 修复 |
| --- | --- | --- |
| P1 | `timeline/service.ts` appendManual schedule 的 HH:MM 只验形状，"25:99" 拼出 Invalid Date → `toISOString()` RangeError → 500 | `parseHHMM` 补真实存在校验（hh≤23、mm≤59）→ 400 |
| P1 | appendManual todo 的 dueAt 无快照防御，非法串 500（confirmPending 同分支 007 已修，此处漏） | `toIsoOr400` 统一预检 → 400 |
| P1 | appendManual finance 金额无上限，巨款落 int4 列 22003 → 500 | 上限 ¥100 万（与 AI 契约 FinanceDraftV2 对齐）→ 400 |
| P1 | manual diet 的 kcal 非整数（22P02）/巨数（22003）→ 500 | 整数 + ≤5000（AI 契约同款），不合法落 null（估不出） |
| P1 | reRecognize 的 todo/finance/people 分支用 `r.time.*` 落库无快照防御，形状合法但日历非法（2025-02-30）→ 500 | switch 前统一 NaN 预检 → 400 |
| P1 | `trades.ts` listTrades 的 page 未钳制：`?page=1.05` → offset 非整数 500；`?page=1e21` → bigint 溢出 500 | trunc + clamp(1, 10000) |
| P1 | transactions POST/PATCH amountCents 无上限 → int4 溢出 500 | 上限 ¥100 万 → 400 |
| P1 | 验证码发送端点（sms/email send）无 IP 级限流，持号码字典可轰炸 | per-IP 15min/20 次 `hitRateLimit`（复用 login-guard 基建） |
| P2 | debts priority 只验整数，1e12 → 22003 → 500 | 限 0~99 → 400 |
| P2 | csv-import 单行巨款导致整批导入事务回滚 | 超限行跳过计入 skips |
| P2 | admin prompts optimize 把上游 e.message 原文回传客户端 | 固定文案 + 原文进日志/审计 |
| P2 | manual/confirmPending schedule 的 activityId 触发复合 FK 23503 → 500 | `assertActivityExists` 预检 → 400「类别不存在」 |
| P2 | debts payments 事务 rollback 无 `.catch`，连接死亡时顶替原始错误 | 补 `.catch(() => {})`（全站对齐） |
| P2 | PATCH 通道绕过 POST 长度上限（todos title ≤200、contacts name ≤30） | PATCH 补同款校验 |
| P2 | goal/people/time 三域非字符串直落 `.trim()`（`{title:123}`）→ TypeError → 500 | 逐处补 typeof 守卫 → 400 |
| P2 | 图片/ASR 上传 formData 全量缓冲后才验大小，超大 body 打内存 | content-length 前置预检（50MB/20MB） |

误报排除：检视组报告的「AI 路径 mood_score 未钳制」经核实 zod 三处契约均已有 `min(-100).max(100)`、规则路径产出常量——非缺陷，不修。

## 二、Web 侧修复

| 级别 | 问题 | 修复 |
| --- | --- | --- |
| P1 | `spaces/[id]/detail` 三处（patchTodo/decompose 内层/pickSpace）+ `use-space-mutations.saveTargetDate` 对非 ApiClientError `throw e` → unhandledrejection 被 ChunkErrorReloader 宽匹配 → **整页强刷丢展开态与输入**（decompose 弱网必现） | 全部收口 `setMsg("网络异常…") + return false`（与 saveEdit 同口径） |
| P1 | `calendar-panel.createBlock` 同款 rethrow → 补录失败整页刷新丢表单 | 收口 return false |
| P1 | `actions-today` 行内编辑保存 due 用宿主时区解析北京墙上串 → 海外设备存错 N 小时 | 改 `localInputToIso`（+08:00 显式解析） |
| P1 | `finance/forms.tsx` TxForm occurredAt 同款时区残留 | kit 新增 `fromLocalInput`（转发 bjInputToIso） |
| P1 | `contacts/interaction-form-modal` 默认时间本地 getter 组串 + 裸 new Date 解析 | `isoToBjInput` / `bjInputToIso` 单源 |
| P1 | **window.confirm 全站迁移收尾（约 14 处内联位）**：actions-today、finance 流水删除/账户归档、finance/debt 归档、home/today-schedule、profile 全端登出、spaces 列表、contacts 详情、space-reflections、moment-feed 五类删除、debt import-drawer 原生 alert | 抽 `useArmConfirm` hook（armed 态 + 3s 超时 + 卸载清理），两步确认/行内提示替换 |
| P2 | todo-board addSubtask 无 busy 守卫，连按 Enter 重复建行动（真数据重复） | `addingSub` 守卫（失败路径也复位） |
| P2 | todo-card 保存按钮无 saving 态；spaces 详情 TodoCircle 未传 disabled；moment-card 确认删除可双击双发 DELETE | `editSaving` / `disabled` / `deleting` 三处补齐 |
| P2 | admin-ai loadVersions 无 seq 守卫，快速切 prompt 显示错版本列表 | `verSeq` 序号守卫 |
| P2 | reminders「☀️ 今日」按钮触控目标 ≈18px | px-2.5 py-1 扩热区 |
| P2 | **生日倒计时宿主时区残留**（shared social.ts dayStart 本地 getter + lunar.ts 同款）：非 CST 设备「今天/明天生日」边界错报 | 北京日历日锚点归一（+8h 后 UTC getter 构造本地零点 holder） |

## 三、Packages 侧修复

| 级别 | 问题 | 修复 |
| --- | --- | --- |
| P1 | `duration.ts parseAmountCents` 不支持千分位："花了1,000元" 匹配到 `000元` → **0 分落库**；无单位分支取到 "1" → 1 元 | 解析前归一 `[,，]`；结果对齐 ¥100 万上限 |
| P2 | `time-infer parseClockRange` 区间终点 "12点" 丢失 pm 语境："晚上10点半到12点" 终点保持正午 → end≤start 跨日 +24h → **块长膨胀 13.5h** | pmish 且终点 hour===12 → 24（午夜） |
| P2 | `parse.ts` 人物去重方向与注释相反：保留长称呼"同事小陈"，与联系人名单对齐语义相反 | 反转 filter 保留短名 |
| P2 | `detectFuture` 不识「月底/月末/下个月/明儿」→ 未来话术被当过去锚定成当天 | 补词表 → "soon" |
| P2 | `schema.ts DOMAINS` 缺 `people`（与 DOMAIN_MODES/domainExtractionV2/DB 六域约束不一致），DOMAIN_LABELS/ICONS 同步补齐 | 枚举单源补全 |
| P2 | `shared/reminders.ts` todo 排序恒 0，与「N 天后 N」契约注释不符，紧迫度信息丢失 | 未过期按北京日历日差排序 |
| P2 | `db/runner.ts` --fresh 基线执行无 try/rollback（失败报"连接失败"误导 + 靠断连隐式回滚）；记录表孤儿（文件缺失）静默跳过 | 基线 try/rollback + 孤儿告警（--status 与执行路径） |
| P2 | schema.sql 被当"现行 schema"阅读会得出错误结论（与迁移终态不同形） | 文件头显式声明"仅作 --fresh 基线" |

不修（记录）：已上线迁移 019/022/027 等的 drop constraint 无 if exists——红线禁止修改已上线迁移；schema.sql 收敛为迁移终态快照属大重构，另起批次。

## 四、工程卫生

- **红线 1 机器化**：eslint 新增 `no-restricted-syntax` 拦 `process.env` 直读（白名单：platform/config.ts、platform/db.ts、middleware、instrumentation、packages/ai），全仓 0 违规
- **env 收编**：FFMPEG_PATH/STATIC_DIR 收进 config.ts（⚠ config 被 instrumentation 以 edge runtime 一并编译，禁 import node 内置模块——路径拼接留给 nodejs 消费方，首次实现曾因此打挂 api 构建）；health 的 UPLOAD_DIR 直读改 config；.env.example 补 12 个缺失变量、删 SUPABASE 死条目
- **死代码清理**：删 web 4 个零引用 shim（lib/bearer、cors、client-api、csv-import）+ shared/api-hooks.ts + 过时脚本 smoke-batch3（**内含明文测试账号口令**）+ test-lunar-birthday.mjs
- **依赖**：apps/api 补声明 zod（9 文件在用靠 hoist 存活）；web 的 solarlunar 保留（tsconfig paths 引用其类型声明）
- **仓库卫生**：`git rm --cached coverage __pycache__`（曾误入库）+ .gitignore 补 `coverage/`、`__pycache__/`、`apps/api/.uploads/`
- README 数字漂移修正（21 组→27 组 API、20→39 迁移）

## 五、目标主线深度体验（GUI 实测）

本地 3100 standalone（AUTH_DISABLED=1，STATIC_DIR=out），种子 `scripts/qa/seed-goal-gui.mjs`（3 空间/7 待办行动/2 感悟，--wipe 可清）。

**PC 1440×900**：
- ✅ 空间列表加载/统计徽标/进度条/天数/目标日期渲染正确
- ✅ 勾选完成 → 轻提示 + 统计 1/3·33% 联动 + 沉底「已完成（1）」折叠区；恢复路径回 0/3
- ✅ 添加行动**连按两次 Enter 仅建 1 条**（本批防重复修复实测通过）
- ✅ **AI 拆解双选项菜单**：已有未完成行动时显示「✨ 重新生成（清空未完成，已完成保留）/ ➕ 追加到末尾」，替代原 confirm「确定=重生成/取消=追加」的不可发现双语义（本批交互重构，实测通过）
- ✅ 行菜单删除 → 原生 confirm → 接受 → 删除成功（回归）
- ✅ 感悟 API 链路、关联已有浮层数据源（未关联 todo 过滤）正确

**移动端 390×844**：
- ✅ 列表/详情**无横向溢出**（scrollWidth 382 = clientWidth 382）
- ❌→✅ **空间头部标题与「← 列表」重叠**（InlineRename 外层 inline-flex 按 max-content 撑开，min-w-0 对非 flex 子项无效）→ 加 `max-w-full`，回归实测 titleRight 240 < listLeft 252，标题正确省略号截断
- ❌→✅ **todo 展开 ▼ 触控目标仅 9×15px** → 负 margin 扩热区（todo-board 同位已有 tap-lg，此处补齐）

**⚠ 重大发现与回退决策（P1）**：portal（createPortal→body）菜单内的**臂起两步确认在 React 19 下不可靠**——菜单打开后经臂起重渲染的同一按钮，第二次点击事件不送达（10+ 次复现、3 种点击通道、elementFromPoint 命中无误、fiber props 目视正确；而**内联**两步删除 100% 可靠，同为自动化全流程验证）。**destructive 操作安全优先**：三处 portal 菜单（spaces 行菜单、spaces 空间菜单、todo-board 行菜单）回退为原生 window.confirm（destructive 路径由 confirm 兜底），内联两步迁移全部保留。根因待查（疑似 React 19 portal 重渲染后事件委托问题），另起编号跟进。

## 六、验证与部署

- typecheck（web/api/ai 三端）0 error；eslint 0 error（含新增红线规则）
- `npm test`：api 98（pass 29，其余无测试库跳过）/ ai **104** / shared 42，**0 fail**
- `npm run poc`：39/46（85%）与基线一致
- `npm run test:routes`：见部署前记录
- `npm run build` 通过；无迁移、无 DB 变更，双 tar 部署 + 冒烟（/login 200、/api/auth/me 401）

## 七、遗留（记录不阻塞）

- **React 19 portal 内重渲染元素点击不送达**（本批回退根因，需最小复现后向上游报告或改用 Radix/floating-ui 类方案）
- 时间块重叠 check-then-insert、复盘 acquireGeneration 无锁、登录时序侧信道（007 遗留延续）
- 迁移文件幂等性统一（019/022/027 do-block 守卫）——已上线不可改，新迁移按 024 式写
- schema.sql 收敛为迁移终态快照；金额解析单源化（ai/shared 各一份）；bj 时间工具多处副本收敛 `@/lib/bj-time`（007/008 连续两轮时区 bug 均源于复制走样）
- 空间创建 MAX_ACTIVE_SPACES check-then-insert 并发可超 1 个（软限制）
- goal/time/insight/platform 域覆盖率门槛外（run-coverage.mjs --include 待扩），shared date.ts 零专测
