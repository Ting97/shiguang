# 004 · 测试与部署记录

> 每批流程：tsc 三端 → eslint(0 error) → 单测 → 构建 → 本地 3100 断言/浏览器验证 → commit+push → 双 tar 部署 → 冒烟（/api/health · /login 200 · 鉴权 401）。
> 每批独立提交、独立可回滚（/opt/shiguangri_old 逐版快照）；DB 变更前 pg_dump 备份。

## 4-A 工程地基（e5080b4，2026-09-23 上线）

- **config fail-fast**：`server/platform/config.ts` + instrumentation.register 启动校验。生产判定用显式 `APP_ENV=production`（实施修正：standalone 的 NODE_ENV 恒为 production，用 NODE_ENV 判定会误伤本地 AUTH_DISABLED 测试流——偏差已回写 02 §3.3）。本地实测：生产+AUTH_DISABLED 拒启 exit 1、两条 issue 清单输出 ✓；服务器 .env 已加 APP_ENV=production（部署前核验其 DATABASE_URL 无弱口令模式、无 AUTH_DISABLED）。
- **迁移 runner**：`packages/db/runner.ts` + 035 schema_migrations。本地三态演练：未初始化提示/自举回填 34 个/幂等"已是最新" ✓；checksum 防手改。生产执行 035 + 回填（applied_by=backfill, 34 行）✓。
- **/api/health**：db/uploadsWritable/aiConfigured/jevConfigured/migrationPending；middleware 放行探活。生产上线即绿。
- **结构化日志**：AsyncLocalStorage requestId + JSON stdout（零依赖 pino 等价实现）。
- **CI/ESLint/Prettier/AGENTS.md**：GitHub Actions lint→typecheck→test→build；ESLint 9 + typescript-eslint（路由层迁移期 warn 项为定向标记）；修复 packages/ai 测试夹具历史类型错与全库 50 处未用变量。

## 4-B 安全加固（f4db940，2026-09-23 上线）

- 034 login_attempts（生产 pg_dump 备份后执行 ✓）+ 双层限流锁定 + CSRF 硬校验（middleware 层集中实现——比 02 设计的 withAuth 内挂更全覆盖，偏差回写）+ logout-all + setup 一次性令牌 + audit 写失败重试。
- **真实登录端到端矩阵 14/14**：跨站写 403 / 同源放行 / 无 Origin 证明 403 / Bearer 豁免 / 连续失败 423 / 清账可登录 / 双会话 logout-all 全吊销 / setup 无令牌 403 + 令牌一次性作废 410 / 模糊文案不暴露账号存在性。security 单测 5 项（滑动窗口滑出解封、CSRF 判定表）。
- 部署：SETUP_TOKEN 已生成写入服务器 .env（不落仓库）。

## 4-C 基座 + 示范域（847ac9e，2026-09-23 上线）

- `server/platform/http`：ApiError（{error,code} 兼容现状）+ withRoute/withAuth/withAuthParams/withAdmin/withModule/withSchema/withAuthSchema/withAuthQuery（zod 400 统一、ZodError 中央映射）。
- identity 域四件套 + 9 路由适配器化；timeline 域 analyze 迁入 + service/repo + parse/confirm/feed/feed[id] 适配器化。
- 回归：auth 10/10；timeline 12 项（发动态秒回/feed 聚合检索/心情/空间归属/越权 400/编辑重识别/金额正确/删除级联/confirm 404）。

## 4-D 其余域结构 + lib 清零（06aa45b，2026-09-23 上线）

- lib/ 21 文件按 02 §2.3 映射迁入九域（identity/timeline/time/finance/debt/insight/ai/platform），73 个引用文件批量改写，**lib/ 目录删除**（AC-1 ✓）。
- FR-B2.1 重复归一：北京时区日期工具单源 packages/shared/date（bjToday/bjAddDays/bjMondayOf）；jev-shadow nowCst 单源 toCstWallClock。
- 全域 15 路由冒烟 ✓。
- **诚实说明**：goal/time/people/insight/ai/platform 的路由层已随域结构组织，但逐路由的 service/repo 三层抽取与 ≤50 行瘦身为**持续项**（identity/timeline 为完整范本；eslint no-restricted-imports 迁移期 warn 已就位，lib 清零后升 error）。

## 4-E AI 内核（966f563，2026-09-23 上线）

- glm.ts 双传输层：GLM_TRANSPORT=sdk（默认，Vercel AI SDK）+ legacy（等价回退开关）。GLM 私有扩展（thinking/reasoning_effort/max_tokens）经 body 补丁 fetch 注入；重试/降级/熔断策略层保留。
- **PoC 实测对照**（glm-5.3-flash live 同参同模型）：SDK 36/46(78%) ≥ legacy 31/46(67%) → 切默认 sdk。规则 PoC 85% 达标。
- 429 结构化判定（APICallError）；ASR 2 次重试；PEOPLE_RE 去演示人名（联系人名单驱动，空名单不产人物）；extractJson 双实现归一；GLM_MODEL 单源 activeModel()。

## 4-F 前端数据层基建（f20939c，2026-09-23 上线）

- SessionProvider/useSession（唯一会话源 + 401 唯一跳转）、useApi、ErrorBoundary、Toast、根布局接线。
- 登录态 GUI 验证：401→/login、登录成功、四页渲染正常。
- **诚实说明**：113 处裸 fetch 收编为渐进项——基建就绪后单点转换为分钟级工作；本批完成导航/会话链路。

## 4-G 巨型组件拆分（31236d7，2026-09-23 上线）

- finance/page.tsx 952→453 行示范拆分：components/finance/{kit,client,forms,display}（全部 ≤300 行）；GUI 回归全链路正常。
- 其余 9 个巨型文件（detail.tsx 1258 / moment-feed 922 / todo-board 875 / page 753 / admin-ai-panel 691 / debt 650 / contacts detail 483…）按同手册拆分，**列为持续项**。

## 4-H 可选增强（P2）

MCP server、036 用量台账、覆盖率门槛收紧——按需求文档标注 P2，未在本轮范围。

## 003 · 3-D Jev 实时接管（86074fd，2026-09-23 上线，跨包记录）

- 混合引擎 parseHybridInput（Jev 闭集 + GLM 瘦身 extract_open_vocab）+ 空间归属切 Jev + 降级链 Jev→GLM→rules；管理台三态开关放开 on。
- 测试：hybrid 单测 6 项 TZ 双跑；端到端 on（engine=jev-hybrid 落库、空间归属 accepted）/off 回归/shadow 影子恢复/弱模型瘦身失败回落/Jev 断网全链回落。
- 修复：jevEnabled 将 env JEV_MODE 混入可用性判断（DB 覆盖 on 被 env=off 挡掉）；jev-shadow 两处宿主时区二次 +8；classifySpace Jev 分支异常穿透（未走 GLM 回落）。
- 生产默认 shadow：切 on 即接管，管理台一键回退。

## 交付物

- **docs/13-产品与架构总览.html**：重构后最新形态的产品介绍、业务架构（9 领域）、技术架构、安全/可观测、部署拓扑、里程碑（自包含单文件）。
