# 003 · 需求设计：AI 输入装配管理 + Jev 接入 + 产品模块迭代

| 项 | 值 |
|---|---|
| 编号 | REQ-003 |
| 状态 | 开发设计中（v1.2） |
| 创建日期 | 2026-09-22 |
| 来源 | 输入装配：用户提出（2026-09-22，原拟并入 002，因其上线另立）；Jev 接入：用户提出（2026-09-22，已获取 API key）；产品模块迭代：用户提出（2026-09-23，含负债管理专项调研） |
| 关联文档 | [02-开发设计.md](02-开发设计.md) · [docs/10-技术调研-Jev决策模型详解](../../10-技术调研-Jev决策模型详解.html)（2026-09-19 首轮调研） · [001-需求设计](../001-动态图片-视觉升级-目标空间-后台管理/01-需求设计.md) R4 · [002-需求设计](../002-空间感悟-待办手动关联-浮层点击关闭/01-需求设计.md)（已上线） |
| 涉及端 | A 部分：Web（/admin）、API、DB（迁移 029）；B 部分：packages/ai、API、服务器配置（零迁移）；C 部分：Web、API、DB（迁移 030/031） |

## 修订记录

| 版本 | 日期 | 说明 |
|---|---|---|
| v1.0 | 2026-09-22 | 初稿：A 部分「AI 输入装配管理」（自 002 包 N7 迁入独立成包） |
| v1.1 | 2026-09-22 | 并入 **B 部分「Jev 接入」**（用户获取到 API key，启动接入）：调研结论矩阵 + 三段式需求（PoC → shadow → 接管，首批范围=空间分类 + 五域闭集字段）；输入装配迁移因 028 编号被 `028-child-kind-repair.sql` 占用改为 **029** |
| v1.2 | 2026-09-23 | 并入 **C 部分「产品模块迭代」**：C1 快修（导航统一/感悟编辑删除/空间加行动/关联动态/账户改名）、C2 模块授权 + 负债管理（主流产品 + trade.ting97.cn/资产 SSH 调研落地）+ 交易复盘、C3 图谱缩放与 label 升级；迁移 030/031；批次 3-E/3-F/3-G |

---

## 1. 背景（A 部分：AI 输入装配管理）

一次 AI 调用的完整输入 = **system prompt**（001 R4 已可在 /admin 在线编辑、版本回滚）+ **user 输入**（模板 + 注入数据）。当前 user 侧全部硬编码在代码里，管理员看不到"每次调用到底把哪些数据给了模型"，更无法调整：

- 识别类（extract_full / 六个单域）：当前时间、分类对照表、已有联系人名单、用户话术；
- 复盘类（day/week/month/year）：当期事实聚合、动态/日程/完成 todo 明细行（有截断上限）、下层小结链（周←日、月←周、年←月）、用户画像块；
- 拆解类（todo/action_decompose）：todo/行动上下文、空间名称与描述、画像块、已有行动清单（去重依据）；
- 其他：profile_merge（旧画像+本月事实+本月复盘）、space_classify（候选空间+动态原文）、prompt_optimizer。

这些注入项直接决定**识别质量、token 成本与隐私面**（给模型多少个人数据）。本需求把它们在后台**透明化 + 可调**，并配零成本的"装配预览"验证手段。

## 2. 用户故事

- 作为管理员，我想知道"发一条动态后模型实际收到了什么"——在后台看到该能力完整的 user 输入构成，而不是猜。
- 作为管理员，我觉得复盘注入的明细行太多、token 花费大，我想把明细上限调小，或关掉画像注入试试效果。
- 作为管理员，我改完配置想先验证拼出来的输入长什么样，**不花 token** 地预览一次装配结果。

## 3. 需求总览

**A 部分：AI 输入装配管理**

| 编号 | 名称 | 一句话描述 | 优先级 | 涉及端 | 规模 |
|---|---|---|---|---|---|
| A1 | 输入构成展示 | 每个 AI 能力在 /admin 展示 user 模板 + 注入项清单（说明/来源/开关/参数/必需性） | P0 | Web、API | 中 |
| A2 | user 模板与注入可调 | user 模板在线编辑（占位符校验）；非必需注入项开关、数值参数可调，保存即生效 | P0 | Web、API、DB | 中 |
| A3 | 装配预览 | 按当前配置真实装配一次 user 输入并展示，不调 LLM、零 token | P1 | Web、API | 小 |
| A4 | 版本与回滚 | system + user 模板 + 配置三件套同一版本快照，整体回滚；启用开关 = 整 key 回退代码默认 | P1 | API、DB | 小 |

**B 部分：Jev 接入（第二 AI 供应商）**

| 编号 | 名称 | 一句话描述 | 优先级 | 涉及端 | 规模 |
|---|---|---|---|---|---|
| B1 | PoC 对照评测 | 用现有 46 句测试集离线验证 Jev 中文闭集判别力，与 GLM 对照，达标才继续 | **P0 前置门** | packages/ai 脚本 | 小（半天） |
| B2 | shadow 影子模式 | `JEV_MODE=shadow` 线上双跑：Jev 结果只写审计对比、行为仍 GLM，统计一致率 | P1 | API | 小 |
| B3 | 接管 | 空间分类切 Jev + 五域提取混合引擎（Jev 管闭集字段与置信度，GLM 只生成开放词汇）；GLM/rules 降级链全保留 | P1 | packages/ai、API | 中~大 |

**C 部分：产品模块迭代（v1.2 并入）**

| 编号 | 名称 | 一句话描述 | 优先级 | 涉及端 | 规模 |
|---|---|---|---|---|---|
| C1 | 体验快修批 | 导航宽度统一、感悟编辑/删除 PC 修复、空间详情可添加行动、空间可关联动态、账户可改名 | **P0** | Web | 小（根因均已定位） |
| C2 | 模块授权 + 财务新模块 | 管理员可授权用户使用指定模块；负债管理（档案/到期/策略模拟）；交易复盘（日/周统计 + AI 周报） | P1 | Web、API、DB | 大 |
| C3 | 图谱升级 | 缩放/平移、名称 label 修复、视觉升级 | P1 | Web | 中 |

## 4. 功能需求（A 部分）

| 编号 | 需求 | 说明 |
|---|---|---|
| FR-1.1 | 输入构成展示 | /admin AI 管理的每个 prompt key 详情扩展三段：**System prompt**（现状）/ **输入装配**（user 模板 + 注入项清单 + 参数）/ **版本历史**。注入项逐条展示：名称、作用说明、数据来源、当前开关、参数值、是否必需（必需不可关） |
| FR-1.2 | user 模板可编辑 | 每个能力的 user prompt 模板在线编辑（默认值 = 代码内置）；保存校验**占位符完整性**：必须包含该能力全部占位符、不得引入未知占位符，违规保存拒绝并提示缺失/多余项（前端提供"一键补齐缺失占位符"） |
| FR-1.3 | 注入开关可调 | 非必需注入项可开关：分类对照、联系人名单、画像注入、下层小结链、已有行动清单、空间上下文等（按能力注册表定义）；关闭后线上装配即不含该块 |
| FR-1.4 | 注入参数可调 | 数值参数在线调整：明细行截断上限（各复盘档）、名单条数上限、候选空间上限、已有行动条数上限等；min/max 范围校验；明细类上限在 SQL LIMIT 层生效（真正省 token） |
| FR-1.5 | 装配预览 | 详情页「装配预览」：可选粘贴样例（动态原文/todo 标题/复盘期段，缺省取管理员最近真实数据）→ 服务端按**当前配置**走与线上同一条装配逻辑 → 返回完整 user 输入展示；**不调用 LLM、零 token**；预览即所见即所得 |
| FR-1.6 | 生效与版本 | 三件套（system/user 模板/配置）同机制：保存即生效（≤60s 缓存、保存主动失效）；每次保存进版本快照，回滚整体回滚；关闭启用开关 = 整 key 回退代码默认 |
| FR-1.7 | 注册表约束 | 可调范围由代码内注册表定义（每 key 声明：默认模板、占位符清单、注入项及必需性、参数范围）；后台只开放注册表内的项，不支持自由增删注入源 |

## 5. 边界与异常（A 部分）

- 必需注入项（识别类"当前时间/用户话术"、画像合并三要素等）锁定不可关——关闭会破坏功能语义。
- 参数下限由注册表声明（多数为 0 = 不注入该块，或 1）；不设奇点默认值。
- 装配预览使用管理员自己的真实数据（分类、联系人、空间、最近明细），结果仅当前 admin 会话可见，不落库、日志不记内容。
- 028 为纯加列扩展（ai_prompts / ai_prompt_versions），存量行为空 = 代码默认，**上线即刻零行为变化**。
- 代码注册表将来新增占位符时，DB 中已覆盖的旧模板在下次保存时被校验拦截并提示缺失项——按提示"一键补齐"或回滚默认。
- 本需求不改各能力的 system prompt 现状能力（001 R4 已上线部分原样保留），仅在其上扩展。

## 6. 验收标准（A 部分）

- [ ] 每个 key 详情页可见 user 模板、注入项清单（说明/来源/开关/参数/必需性）
- [ ] user 模板可编辑；删占位符/加未知占位符的保存被拒绝并明确提示；一键补齐后可保存
- [ ] 关闭某注入项、调小某参数后：装配预览对应块消失/截断变化，且线上同口径生效
- [ ] 装配预览不产生 token 消耗（audit_logs 无新 LLM 调用记录）
- [ ] 保存后 ≤60s 生效；版本快照含三件套，回滚整体恢复；关闭启用开关整 key 回退代码默认
- [ ] 存量（未配置的 key）行为与当前线上一致（逐能力回归：识别/单域/复盘四档/拆解/空间分类）

---

## 7. B 部分·调研结论：Jev 是什么、能用在哪里

> 首轮调研见 [docs/10-技术调研-Jev决策模型详解](../../10-技术调研-Jev决策模型详解.html)（2026-09-19，结论"暂不接入、列观察清单"）。本轮（2026-09-22）已获取 API key、在线核实 API 形态，并按滚动吸收规则启动接入。

### 7.1 Jev 能力与 API（2026-09-22 在线核实）

- TypeSafe AI 公司的首个「System One 模型」（`jev-latest`）：**不生成任何文本**，只对输入 state 回答**闭集问题**，返回类型化答案 + **校准概率分布**（官方 RLCD 训练，宣称概率可信可设阈值）。
- API：`POST https://api.typesafe.ai/v1/systemone`，`Authorization: Bearer <key>`；请求 = `{ state, model, questions }`，三种问题原语——`choice`（选项选择）、`score`（有序等级评分）、`noul`（是非概率）；响应每问带 `confidence` 与全选项 `probabilities`；同一 state 的多问题**一次调用并行评估**（加问不增延迟）。
- 价格：输入 $0.042/M token、**输出免费**；官方口径单次判断约 $0.0004，比 LLM 快两个数量级（193.6×/444.6× 为厂商自测）。
- 限制：**无文本生成**（标题/摘要/复盘/追问全不能）；**开放词汇抽取不能**（抽不出"王智聪"这样的人名）；**多语言/中文支持官方未声明**——中文口语判别力无任何公开实测，这正是 B1 PoC 要回答的问题。

### 7.2 产品 AI 调用点适配矩阵（11 个调用点逐一判定）

| 调用点 | 现状（GLM） | Jev 适配度 | 结论 |
|---|---|---|---|
| **空间分类**（`analyze.ts` classifySpace，每次发布后一次 chat） | Choice 型判断 + 自报 confidence ≥0.7 才写入 | **完美**：Noul（是否归属某空间）+ Choice（候选空间列表）一次调用；校准概率直接当 0.7 阈值 | ✅ 首批接管 |
| **五域提取**（extract_full，发动态主链路） | 一次大 prompt 全字段输出 | **约六成字段完美**：各域适用性=Noul、活动分类=Choice（criteria 动态注入用户自定义分类）、过去/未来=Choice、时段=Choice、心情有无+强度=Noul+Score、财务方向/分类=Choice（金额数值本就由确定性引擎解析）、饮食餐次=Choice；**人名/标题/饮食条目等开放词汇=不能**（保留 GLM） | ✅ 混合引擎（Jev 闭集 + GLM 开放词汇） |
| 置信度来源（0.6 阈值 gating） | LLM 自报 confidence（不可靠） | Jev probabilities 校准，可整体替换置信度来源 | ✅ 随五域接管 |
| 单域重识别（entries/recognize） | chat 单域 prompt | 闭集部分同上（依赖混合引擎基建） | ✅ 随五域接管 |
| 规则兜底（rulesPipeline） | GLM 失败时的正则引擎 | 可作 GLM 与 rules 之间的第二降级层 | ⚪ 可选，暂不做 |
| 复盘四档（review day/week/month/year） | 生成 summary/highlights/suggestions | ❌ 生成任务 | 维持 GLM |
| 画像合并（profile_merge） | 生成 | ❌ | 维持 GLM |
| AI 拆解（todo/action_decompose） | 生成行动标题 | ❌ | 维持 GLM |
| prompt 优化（prompt_optimizer） | 生成优化稿 | ❌ | 维持 GLM |
| 联系人 AI 画像（ai-profile） | 生成 | ❌ | 维持 GLM |
| ASR（语音转写） | 专用模型 | ❌ | 维持 GLM-ASR |

### 7.3 价值与成本

- 直击两大既有痛点：**免费档晚高峰拥塞**（429 熔断时有发生，Jev 分流高频闭集判断）与 **LLM 自报置信度不可靠**（校准概率让 0.6/0.7 阈值真正有意义）。
- 成本：按 docs/10 估算月 1.7M 输入 token ≈ **¥0.5/月**，输出免费；双引擎不增加用户侧配额负担（配额口径见 FR-B3）。

### 7.4 docs/10「暂不接入」四条理由的复核

| 当时理由 | 现状 |
|---|---|
| ① 中文能力无实测佐证 | **仍未解决** → B1 PoC 作为前置门，不达标即止步归档 |
| ② 上线仅数日、依赖风险高 | 仍未消除 → 三段式 + GLM 降级链全保留 + `JEV_MODE` 一键关 |
| ③ 只覆盖六成字段、引入双引擎复杂度 | 接受复杂度（用户已选首批即含混合引擎），用编排层收敛 |
| ④ 拥塞痛点可用 GLM 付费档解决 | 付费档仍要钱、且置信度问题付费解决不了 → Jev 两者兼顾 |

## 8. 功能需求（B 部分：Jev 接入，三段式）

| 编号 | 需求 | 说明 |
|---|---|---|
| FR-B1 | **PoC 对照评测（前置门）** | 复用 `packages/ai/testset` 46 句真实测试集，把五域闭集字段改写为 Jev 问题组离线跑批，与 GLM 结果（poc:live 基准）逐字段对照。**达标线：闭集字段整体准确率 ≥ GLM×95%，且活动分类单独 ≥80%**；不达标则止步，结论归档进本包（不算失败，是复评结论落档） |
| FR-B2 | **shadow 影子模式** | 服务器 `JEV_MODE=shadow`：线上每次空间分类/五域识别后，fire-and-forget 用同题调 Jev，结果只写 `audit_logs`（stage='jev_shadow'，记一致/不一致），**用户行为零变化**；持续 ≥1 周统计一致率，**空间分类一致率 ≥90% 且五域闭集 ≥85%** 才进入接管 |
| FR-B3 | **接管（首批范围：空间分类 + 五域闭集）** | `JEV_MODE=on`：① 空间分类整切 Jev（Noul+Choice 一次调用，probabilities ≥0.7 写入）；② 五域提取改混合引擎——Jev 问题组出闭集字段与各域 confidence，GLM 瘦身 prompt 只生成开放词汇（标题/人名/饮食条目/时间原文），确定性引擎（时长/金额/日期锚定）不动；③ **checkAiQuota 口径修正：排除 model 为 jev\* 的审计行**（Jev 极廉不计用户配额，也防 shadow 双跑把 30 次/月配额烧穿）；④ 降级链保留：Jev 失败 → 现行 GLM 全量路径 → rules，用户无感；⑤ `ParseResult.engine` 新增 "jev-hybrid" 枚举，前端 ⚠ 离线识别 UI 兼容 |
| FR-B4 | 配置与安全 | env：`TYPESAFE_API_KEY`（仅服务器 `.env`，**永不入库**）/`TYPESAFE_BASE_URL`/`JEV_MODEL`/`JEV_MODE`（off/shadow/on，默认 off）；key 已在聊天记录中出现过，**接入前先在 console.typesafe.ai 轮换** |

## 9. 边界与异常（B 部分）

- Jev 无中文声明：PoC/shadow 两道门都是为此设的；任一门不达标即停，已上线部分可 `JEV_MODE=off` 秒退。
- 混合引擎下 GLM prompt 需瘦身改版（不再输出闭集字段）——prompt 变更走 A 部分的 /admin 版本管理，可回滚。
- Jev 调用超时 5s/重试 2 次后即降级 GLM，不阻塞发布主链路（空间分类本就异步静默）。
- 审计：Jev 行 `model='jev-latest'`，shadow 行不计配额也不计入 tokens/usage 的 GLM 成本统计口径。

## 10. 验收标准（B 部分）

- [ ] PoC 报告产出：46 句逐字段对照表 + 准确率汇总，达标线判定结论明确（达标/止步两出口均算验收通过）
- [ ] shadow 期间用户行为零变化（抽查动态归属与五域产物与 GLM 版一致）；audit 可统计一致率
- [ ] 接管后：空间归属抽检与 GLM 版一致率达标；五域闭集字段来自 Jev、开放词汇来自 GLM、confidence=probabilities
- [ ] Jev 不可用时自动回落 GLM 全量路径（断网/错 key 模拟），用户无感，audit 有降级记录
- [ ] 用户 AI 配额消耗不因 Jev 双跑/接管而增加（口径修正验证）
- [ ] `JEV_MODE=off` 一键回退全 GLM 行为
- [ ] 仓库任何文件 grep 不到 API key 明文

---

## 11. C 部分·背景与调研（产品模块迭代）

### 11.1 问题清单与根因（2026-09-22 代码盘点）

| # | 问题 | 根因（已定位） |
|---|---|---|
| 1 | PC 导航切换时项长度不一 | nav.tsx 五项共用 linkCls，**激活态追加 `font-medium` 使文字变宽**，切换时该项（及居中布局的整行）跳动；各项无统一 min-width |
| 2 | PC 端感悟不能编辑/删除 | space-reflections.tsx 行容器 `<li>` **缺 `group` 类**，`row-actions-hidden group-hover:block` 永不触发（触屏 hover:none 反而常显）——纯 CSS 缺陷 |
| 3 | 空间详情页不能给 todo 新增行动 | 详情页只有"AI 拆解/关联已有 todo"，**没有手动添加行动的输入 UI**（接口 `POST /api/todos {title, parentId}` 本就支持） |
| 4 | 空间下不能关联未归属动态 | 详情页动态 tab 只读展示已归属动态；后端 `/api/feed?spaceId=none`（未归属过滤）已有、前端无入口；手动归属入口只在首页动态卡片菜单 |
| 5 | 账户不能改名 | `PATCH /api/accounts/[id]` **已支持 name/icon**，前端 AccountManager 只做了期初余额编辑与归档，缺名称/图标编辑 UI |
| 6 | 图谱不能缩放、名称被挡 | 手写 SVG 固定 viewBox：无缩放平移；label 为无衬底 `<text>`（被连线穿过/相邻互叠），布局边距未计入 label 高度（**外圈下方名字被 viewBox 下边界裁掉**） |

### 11.2 调研一：trade.ting97.cn/资产（2026-09-23 SSH 核查）

服务器 `/opt/aitrade/资产/` 为一套"资产还款仪表盘"（静态页 + data.js 单一数字源 + Node 接口），可抽象为通用能力的部分：

| trade 实现 | 抽象为拾光「负债管理」能力 |
|---|---|
| 贷款清单：优先级(A1-E1)/银行/本金/利率/还款方式(先息后本·等额本息·分期)/期限/月供/还款日/分组(balloon/amort/card/family)/备注含策略提示 | **负债档案**：类型分组（信用卡/房贷车贷/消费贷/花呗白条/亲友借款）+ 上述字段 + 优先级 + 备注 |
| balloonNodes 到期节点：到期月/金额/danger·warn·bad·ok 分级/行动建议（"提前续贷"） | **到期墙**：先息后本/临期负债的到期倒计时列表，分级警示 + 提前提醒 |
| 多口径汇总（银行债/含亲友/先息后本总额/等额总额）与月供跳升时间线 | **总览卡**：总负债（银行口径/含亲友口径）、月供合计与未来跳升、加权利率、净资产（资产-负债） |
| 每月现金流计划 / 每月需赚多少（月供+生活开支 vs 收入 → 缺口） | **现金流月视图**：月供 vs 当月真实收支（拾光有 transactions 真实数据，可自动算）+ 缺口提示 |
| 还款进度 / snapshots 快照 | **还款记录与进度**：逐笔还款 + 进度可视化（本金余额曲线） |
| 备付追踪 / C2C 可视化 / 黄金 CFD / 具体个人数字 | **不搬运**（个人化/投资内容与拾光定位无关） |

### 11.3 调研二：主流个人负债管理产品（2026-09-23）

综合 债务清零(App Store)、Debt Payoff Planner / Debt Snowball(Google Play)、Undebt.it、随手记系等的共性功能模型：

1. **债务档案**：每笔余额、年化利率、最低还款额、还款日；多类型（信用卡/贷款/医疗/亲友）；
2. **还款策略引擎**：**雪球法**（先还小额，心理激励）vs **雪崩法**（先还高息，总利息最少）并排对比；
3. **还款计划模拟**：输入每月额外还款额 → 生成还款时间表、**预计清零日期**、利息节省估算；
4. **进度与激励**：还款进度可视化、清零倒计时、里程碑；
5. **与收支联动**：循环账单提醒、"剩余可用"视图。

拾光落地取舍：①+②+③+④ 全部纳入；⑤ 用拾光现有 transactions/预算体系天然承接（现金流月视图）。

### 11.4 权限现状

`profiles.role` 仅 admin/user；plan(free/pro) 只控 AI 配额。**无任何"管理员授权某用户使用某模块"机制**（无表无字段无 API）——C2 需先建模块授权基建。

## 12. 功能需求（C 部分）

### 12.1 C1 体验快修批

| 编号 | 需求 | 说明 |
|---|---|---|
| FR-C1.1 | 导航宽度统一 | 五个导航项宽度一致（固定 min-width + 居中文本），激活/非激活切换**不改变占位宽度**（消除跳动）；以"目标"项为基准；顺带统一过渡与视觉（激活渐变、hover 一致） |
| FR-C1.2 | 感悟编辑/删除修复 | PC 端 hover 显示编辑 ✏️ /删除 🗑（修复 group 类缺失）；删除保留确认；移动端维持常显 |
| FR-C1.3 | 空间详情可添加行动 | 空间内每个未完成 todo 的行动区提供"添加行动"输入行（回车保存，`POST {title, parentId}`）；行动继承空间归属 |
| FR-C1.4 | 空间关联动态 | 详情页动态 tab 增「🔗 关联动态」入口：浮层列出**未归属动态**（`spaceId=none`，支持搜索/分页）→ 点选即关联到当前空间；关联后列表即时更新 |
| FR-C1.5 | 账户改名 | 账户管理弹层：名称可编辑（重名校验同现有 API 行为）、图标可改（复用 ICONS 选择） |

### 12.2 C2 模块授权 + 负债管理 + 交易复盘

| 编号 | 需求 | 说明 |
|---|---|---|
| FR-C2.1 | 模块授权机制 | 新增**模块级授权**：admin 默认全模块可用；admin 可授权指定用户使用指定模块（首期模块：`debt` 负债管理、`trade_review` 交易复盘）；被授权用户才可见/可用对应模块（导航 tab 与 API 双层门禁）；授权管理入口在 /admin |
| FR-C2.2 | 负债档案 | CRUD：名称、类型（信用卡/房贷/车贷/消费贷/花呗白条/亲友借款）、本金余额、年化利率、月供、还款日、到期日/结清日、优先级、备注、状态（进行/已结清/已归档） |
| FR-C2.3 | 还款记录与进度 | 逐笔记录还款（金额/日期/可选关联资产账户自动记一笔支出）；进度 = 本金余额曲线 + 已还/剩余；结清状态自动/手动标记 |
| FR-C2.4 | 到期墙与提醒 | 临期负债到期倒计时列表（分级警示：≤3 个月 danger / ≤6 个月 warn）；先息后本类型突出"本金到期"节点 |
| FR-C2.5 | 总览与现金流 | 总负债（银行口径/含亲友）、月供合计与**未来跳升提示**、加权利率、净资产（Σ资产账户余额 − 总负债）；现金流月视图：本月月供 vs 真实收支 → 缺口/盈余 |
| FR-C2.6 | 策略模拟 | 输入"每月额外还款额" → **雪球 vs 雪崩**两种顺序并排模拟：逐月还款表、预计清零时间、总利息对比与节省额 |
| FR-C2.7 | 交易复盘模块 | ① 统计：日/周维度交易聚合（总收支、分类分布、账户分布、环比上周/昨日）；② AI 周报：每周交易复盘卡片（结构化摘要+亮点+建议，复用现有复盘管线模式：缓存+配额+prompt 可调） |
| FR-C2.8 | 财务页信息架构 | finance 页顶部加二级 tab（概览 \| 负债 \| 交易复盘），后两者为新路由页面；**仅授权用户可见 tab**，未授权不渲染入口且 API 403 |

### 12.3 C3 图谱升级

| 编号 | 需求 | 说明 |
|---|---|---|
| FR-C3.1 | 缩放与平移 | 图谱整体可缩放（滚轮以指针为中心缩放、双指 pinch）、可平移（背景拖拽）、双击复位、提供 +/−/⟲ 控制按钮；缩放范围 0.4~2.5；节点拖动与平移不冲突（点节点=拖节点，点空白=平移） |
| FR-C3.2 | 名称 label 修复 | label 加背景衬底（描边式底色），不再被连线穿过/与轨道圈叠字；布局边距计入 label 高度，**外圈靠下名字不再被裁剪**；相邻 label 重叠时错位；缩放低于阈值时隐藏 label 保持画面干净 |
| FR-C3.3 | 视觉升级 | 中心节点光晕、"我"标识强化、连线按互动热度渐变、hover 节点高亮其连线、分组配色与图例对齐（GROUP_COLOR）、状态切换有过渡动画 |

## 13. 边界与异常（C 部分）

- 负债金额单位统一 cents（与 accounts/transactions 一致）；利率存百分数（0~36 上限校验）；亲友借款利率可为 0、月供可空。
- 策略模拟为**简化模型**：按月复利、月供固定、额外还款额固定，不考虑提前还款违约金/利率变动；页面标注"模拟估算，仅供参考"。
- 还款联动记一笔支出为**可选**开关（避免与既有账单导入/手动记账重复记账）。
- 模块授权只影响**新模块**可见性，现有模块（动态/目标/日程/人际/财务概览）不引入授权。
- 交易复盘 AI 周报走 AI 配额（与现有复盘同池）；统计部分纯 SQL 零 AI 消耗。
- 图谱缩放后节点点击/拖拽命中区域随缩放正确换算；触屏与鼠标行为一致。
- C1 各修复不改变既有数据与接口语义（纯 UI 层）。

## 14. 验收标准（C 部分）

- [ ] 导航五项等宽、切换零跳动；深浅主题视觉统一
- [ ] PC hover 与移动端均可见感悟编辑/删除且功能正常（编辑回填全文、删除有确认）
- [ ] 空间详情页可给未完成 todo 添加行动（立即出现在行动列表且随空间）
- [ ] 空间详情页可浏览未归属动态并关联；关联后计数/列表更新；首页动态卡片归属菜单行为不回退
- [ ] 账户可改名改图标；重名被拒；流水展示的账户名同步更新
- [ ] 非授权用户看不到负债/交易复盘 tab，直连 API 返回 403；admin 全可见；授权后即时生效（重新登录/刷新）
- [ ] 负债档案 CRUD、还款记录、进度曲线、到期墙分级、总览多口径、现金流缺口、雪球/雪崩模拟（含额外还款输入）全部可用
- [ ] 交易复盘：日/周统计正确（与流水对账）、AI 周报生成且缓存/配额生效
- [ ] 图谱：滚轮/pinch 缩放、拖拽平移、双击复位、按钮可用；label 不被线穿过/不裁剪/低缩放隐藏；hover 高亮；深浅主题正常
- [ ] C1+C3 全部页面移动端 PWA 回归无破版

## 15. 非目标（本期不做）

| 项 | 说明 |
|---|---|
| 自由增删注入源 / RAG | 只开放注册表内的开关与参数；新注入源随代码演进 |
| system prompt 的行为变更 | 001 R4 已上线能力原样保留 |
| 试运行（真实调用 LLM 验证输出） | 预览只做输入装配；带真实调用的 test 维持二期可选 |
| 多管理员协作/审批流 | 单管理员体系 |
| Jev 接管生成类触点（复盘/画像/拆解/优化/ASR） | 生成任务 Jev 硬限制不能，永远维持 GLM |
| 人名/标题等开放词汇强行 Jev 化 | 硬限制；混合引擎中保留 GLM 生成 |
| Jev 管理页/UI 开关 | 一期 env（`JEV_MODE`）控制，后台可视化二期再议 |
| Jev 作为独立第二降级层（GLM→Jev→rules 链） | 本期 Jev 只在前向接管位；降级方向仍走 GLM→rules（见 02 §7.6） |
| 负债多币种/投资资产追踪（证券/加密） | 只做负债与净资产口径；投资资产另立需求（trade 中相关内容已明确不搬运） |
| 提前还款违约金/利率变动精算 | 策略模拟为简化月复利模型，页面标注估算口径 |
| 授权体系泛化（角色/细粒度权限） | 只做"admin + 按模块授权"两级，不引入角色树 |
| 图谱力导向重排/自动避让布局引擎 | 保留自有星型轨道布局语义，本期只加缩放平移与 label 修复 |

## 16. 风险

| 风险 | 对策 |
|---|---|
| 配置调错导致识别/复盘劣化 | 三件套整体版本回滚 + 启用开关秒切默认；装配预览零成本验证；必需项锁定 |
| 代码占位符演进与 DB 旧覆盖模板脱节 | 保存时完整性校验拦截 + 一键补齐；注册表为唯一事实源 |
| 预览接口泄露个人数据 | 仅 admin 会话、结果不落库、日志不记内容 |
| **Jev 中文判别力未验证** | B1 PoC 前置门 + B2 shadow 双门槛；任一不达标即止步，`JEV_MODE=off` 秒退 |
| **厂商极新（上线数日）、价格/能力可能变化** | Jev 仅承担"可降级"的闭集判断位；GLM 全量路径作为永久兜底保留；成本月 ¥0.5 级损失可控 |
| **API key 已在聊天记录中出现** | 接入前控制台轮换；仅存服务器 `.env`；仓库 grep 验收（FR-B4/§10） |
| **probabilities 校准假设在中文上不成立** | shadow 期专门统计"Jev 高置信但 GLM 不一致"比例，超 5% 视为校准失效信号 |
| 负债数据敏感（授权泄露面扩大） | 模块授权双层门禁（UI 隐藏 + API 403）；授权操作在 /admin 留痕 |
| 策略模拟误导用户 | 明确"简化模型估算"标注；不做自动扣款/提醒执行类功能 |
| 图谱缩放改造回归面（节点拖动/点击冲突） | 指针事件按目标分流（节点=拖节点，背景=平移）；触屏鼠标双端回归 |
| 财务页结构大改影响既有功能 | 概览页原内容不动只加二级 tab；流水/导入/预算回归 |

## 17. 里程碑

| 批次 | 内容 | 迁移 | 验收门 |
|---|---|---|---|
| 3-A | **A 部分：AI 输入装配**——029 加列 → 注册表/装配器 → /admin 三段详情 + 装配预览 → 逐能力接入 | 029-ai-input-config.sql（028 已被 child-kind-repair 占用） | §6 AC |
| 3-B（可与 3-A 并行） | **B1：Jev PoC 对照评测**（jev.ts 客户端 + run-jev-poc 脚本 + 46 句跑批报告） | 无 | §10 PoC 项 |
| 3-C | **B2：shadow 影子模式上线**（JEV_MODE=shadow 双跑审计，观察 ≥1 周） | 无 | §10 shadow 项 |
| 3-D | **B3：接管**（空间分类切 Jev + 五域混合引擎 + 配额口径修正） | 无 | §10 接管项 |
| 3-E（可先行快赢） | **C1：体验快修批**（导航统一/感悟编辑删除/空间加行动/关联动态/账户改名——根因均已定位的小改） | 无 | §14 C1 项 |
| 3-F | **C2：模块授权 + 负债管理 + 交易复盘**（030/031 → 授权基建 → 负债档案/还款/到期/总览/模拟 → 交易统计与 AI 周报 → 财务二级 tab） | 030-module-grants.sql、031-liabilities.sql | §14 C2 项 |
| 3-G | **C3：图谱升级**（缩放平移 + label 修复 + 视觉） | 无 | §14 C3 项 |

> A/B/C 三线批次相互独立可交错：3-E/3-G 纯前端可随时插队；3-F 建议在 3-E 之后（财务页结构改造基于修好的基线）。3-B 不依赖 3-A；3-D 依赖 3-B/3-C 双达标且建议在 3-A 之后。每批完成走既定构建 → 双 tar 部署 → `/login` 冒烟，记录进本目录 `03-测试与部署.md`。
