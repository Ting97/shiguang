# 002 · 测试与部署记录

| 项 | 值 |
|---|---|
| 状态 | 已全部上线（2-0 / 2-A / 2-C / 2-B 四批，2026-09-22） |
| 关联 | [01-需求设计.md](01-需求设计.md) · [02-开发设计.md](02-开发设计.md) |
| 提交链 | 47ce712(文档) → 500e632(2-0) → fd34d38(2-A) → b9aa6a4(2-C) → 2bb3061(2-B) |

> 批序说明：按 02 文档建议 2-A 先行、2-C 先于 2-B（先定 kind 语义）；另有用户临时报告的「空间加载卡死」bug 作为 2-0 插在最前。每批均：tsc 双端 → 构建 → 本地 3100 浏览器/断言验证 → commit+push → 双 tar 部署 → /login+API 冒烟。

## 批次 2-0：空间加载卡死修复（500e632）

- **根因**：①主因——部署双 tar 换血后，已打开的旧会话按旧 chunk 清单懒加载路由 → 404 → `loading.tsx` 永久兜底；②次因——`spaces/[id]/detail.tsx` 的 `load()` 无 try/catch，fetch 抛错即永远"加载中"（contacts 孪生页有处理，spaces 漏网）。
- **修复**：`components/chunk-error-reloader.tsx`（error/unhandledrejection 监听 chunk 失败特征 → sessionStorage 60s 守卫 → 自动 reload 一次，挂 root layout 覆盖全站）；detail `load()` 补 try/catch/finally + 「加载失败 + 重试」错误态。
- **验证**：注入 chunk 失败事件 → 守卫标记落位 + 自动 reload ✓；断开 API → 错误态+重试按钮渲染 ✓（shots/2026-09-22-08-detail-err.png）。
- 部署后冒烟：active / login 200。

## 批次 2-A：N3 浮层统一关闭 + N5 术语统一（fd34d38）

- **N3**：新建 `dismissable.tsx`（useDismiss/Dismissable：document pointerdown 外部 + Esc，挂载后生效防误关）。接入：识别菜单（EntryMenu 内置）、发布抽屉（含未保存 toast）、todo-board（添加行展开/行内编辑/行动面板/子任务输入/⋯菜单 portal）、moment-feed（⋯菜单 portal/原文编辑）、detail（⋯菜单 portal/描述展开）、icon-picker、finance（Modal/账单导入/流水编辑/确认入账条/全部入账条/预算编辑）、空间编辑弹层。编辑器类取消且内容有改动 → 「已取消，未保存」轻提示。
- **豁免**：图片 Lightbox（已完备）、window.confirm、上传/AI loading；admin 建议面板判定为内联对比区**豁免**（点外部丢弃 55s 的 AI 产出有害，放弃走显式按钮）。
- **N5**：用户可见"待办"清零（→ todo，约 50 处：Web 界面/空态/confirm/菜单/API message/shared 提醒横幅/Expo 3 处）；"子任务"→"行动"；大写 TODO 小写化（tab/页头/横幅 title）；复盘四路由 prompt 素材词同口径（"完成 todo：N 件"）；JSX 注释顺带清理。
- **验证**：浏览器实测——首页副标题/搜索占位/清单 tab 小写 todo ✓；entry 菜单点外部关闭 ✓；添加行展开→点外部收起 ✓；发布抽屉 Esc 关闭 + 「已取消，未保存」toast ✓；grep "待办|子任务" 用户可见清零 ✓。触屏（hover:none 常显）行为不受影响。
- 部署后冒烟：active / login 200 / api 401。

## 批次 2-C：N6 行动解耦（b9aa6a4）

- **027-action-kind.sql**：todos 加 `kind`（'todo'/'action'）+ 子待办回填 + (user_id, kind) 索引；本地与生产执行（生产先 pg_dump todos 表备份），生产行动 11 条回填 ✓。
- **API**：POST 支持 `kind:'action'` 独立行动（可带 today）；parentId 校验收紧父须 kind='todo'；today-actions v2（left join，行动六条件含过期；顶层 todo 保留今日到期含 done/过期未完成）；decompose 按 kind 分流（独立行动 → ≤3 独立行动插入其后，sort 平移圈定无父行动）；PATCH repeatDaily 校验改按 kind（独立行动可设重复）。
- **前端**：actions-today 顶部「添加行动」行（回车即建、默认今日、乐观刷新）+ 独立行动渲染（无父上下文行、「行动」徽标）+ 行操作（✏️ 编辑（Dismissable）/🗑 删除）；todo-board 顶层行「行动」徽标、独立行动无"添加行动"入口。
- **断言（6/6 PASS）**：创建独立行动 / 今日清单含独立行动（parent_title=null）/ 看板全部视图含（kind=action）/ 给独立行动添加子行动 400 / 独立行动设每日重复 / 独立行动设重要且重要视图可见。
- 部署后冒烟：active / login 200 / api 401。

## 批次 2-B：N1 行级关联空间 + N2 空间感悟 + N4 就地重命名（2bb3061）

- **026-space-reflections.sql**：space_reflections 表（content 1~50000 字 check、随空间级联、(space_id, created_at desc) 索引）；本地与生产执行 ✓。
- **N2 API 五路由**：列表（预览 300 字 + chars + edited + 分页，**不回全文**）、全文、POST/PATCH（trim 非空 400、char_length≤50000 400）、DELETE；归属链 JOIN 校验。
- **N2 前端**：空间详情改三 tab（待办/感悟/动态，FilterChip）；SpaceReflections 列表（日期分组、line-clamp-4、点条目拉全文、✏️🗑 行操作、加载更多）；ReflectionEditor 底部抽屉（码点字数 n/50000、粘贴截断、超限禁存、取消有改动轻提示）；删除空间确认文案含感悟/todo/动态计数。
- **N1**：PATCH spaceId 规则（归档空间新关联 400 / 有父行动 409 / null 恒允许 / 响应带 space 摘要）；SpacePicker 浮层（active 列表、归档置灰仅移除、点空白关闭）；todo-board 顶层 ⋯ 菜单「🎯 关联空间」；详情页待办行同接入；关联后详情统计重拉。
- **N4**：InlineRename 组件（Enter 保存 / 点空白 Esc 取消 / ≤40 字）；挂空间列表卡片标题（拦截 Link 跳转）与详情头部。
- **断言（N2 9 项 + N1 5 项 PASS）**：创建/空内容 400/超限 400/列表预览字数/编辑/已编辑标记/全文/删除/删空间级联；归档空间新关联 400/有父行动 409/关联成功带摘要/移除关联/独立行动关联。
- 部署后冒烟：active / login 200 / api 401。

## 已知限制与后续

- 生产登录态的端到端截图验证受 IAB 浏览器会话老化影响，2-C/2-B 的 UI 渲染以「API 断言 + 构建产物 grep + 2-A 同模式已验证渲染」覆盖；建议日间人工走查一遍空间详情三 tab 与首页添加行动。
- 真实注册→验证码→登录流程仍建议人工补测（AUTH_DISABLED 无法黑盒覆盖）。
- 遗留池剩余项（拖拽排序、亲密度规则、邀请码作废、时间轴压缩等）见 `docs/experience/STATE.md`。
